// // src/mixins/alertMixin.ts
// import { Vue, Component, Mixins } from 'vue-property-decorator';
// import { Notify } from 'quasar';

// @Component
// export class AlertMixin extends Vue {
//   showAlert(message: string, type: 'positive' | 'negative' | 'warning' | 'info' | 'ongoing' = 'info') {
//     Notify.create({
//       message: message,
//       type: type,
//       position: 'top',
//       timeout: 2500
//     });
//   }
// }

// export default AlertMixin;
